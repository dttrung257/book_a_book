export interface BookInfoBrief {
  id: number;
  name: string;
  image: string;
  author: string;
  sellingPrice: number;
}
export interface BookInfo {
  id: number;
  name: string;
  image: string;
  author: string;
  sellingPrice: number;
  rating: number;
}

